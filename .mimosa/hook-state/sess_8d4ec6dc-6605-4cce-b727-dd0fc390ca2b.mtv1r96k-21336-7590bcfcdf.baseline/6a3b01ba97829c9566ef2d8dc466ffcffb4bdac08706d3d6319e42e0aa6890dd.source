import { defineConfig } from 'vite';

export default defineConfig({
  base: './',
  server: {
    port: 5173,
    host: '127.0.0.1',
    watch: {
      // 忽略工具产生的临时锁文件/状态目录（扫描中消失的文件会让 watcher 崩溃退出）
      ignored: ['**/.mimosa/**'],
    },
  },
  build: {
    target: 'es2020',
    assetsInlineLimit: 0,
  },
});
